<%= form_for(@sushi) do |f| %>
  <% if @sushi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sushi.errors.count, "error") %> prohibited this sushi from being saved:</h2>

      <ul>
      <% @sushi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
